# Argo Credentials
ARGO_USER=ac.yourname

# BV-BRC Credentials (if needed for API access)
# BVBRC_USERNAME=your_username
# BVBRC_PASSWORD=your_password

# Optional: Override config.yaml settings
# LLM_MODEL=claudesonnet5
# RAG_COMMAND=/path/to/rag.py
